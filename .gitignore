# Dependencies
node_modules/

# Next.js build
.next/
out/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment
.env
.env.local
.env.*.local

# TypeScript
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
!.vscode/extensions.json
.idea/

# Claude personal overrides
.claude/settings.local.json
.claude/.last-used*
.claude/projects/

# Public uploads (runtime data)
public/uploads/*
!public/uploads/.gitkeep

# Expo (mobile)
apps/mobile/.expo/
apps/mobile/dist/
apps/mobile/web-build/
apps/mobile/expo-env.d.ts
apps/mobile/.metro-health-check*

# Native (mobile)
apps/mobile/ios/
apps/mobile/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem

# Evals del bot (datos locales, no commitear)
scripts/bot-eval/reports/*
!scripts/bot-eval/reports/baseline.json
scripts/bot-eval/cases/reales.local.ts
scripts/bot-eval/real/

# Credencial FCM V1 (cuenta de servicio de Firebase) — SENSIBLE, se sube a EAS,
# NUNCA al repo. Ojo: google-services.json SÍ es commiteable (solo lleva
# identificadores públicos), por eso no se ignora aquí.
**/*-firebase-adminsdk-*.json
**/service-account*.json
**/firebase-service-account*.json

# Auditoría técnica: documento de trabajo, no se publica en el repo público.
docs/auditoria-riesgos-mejoras.html
docs/auditoria-riesgos-mejoras.pdf

# Índice local compartido por Codex y Claude Code.
.graphrag/
